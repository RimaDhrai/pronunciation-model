{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$input = $input | Out-String | ConvertFrom-Json; $f = $input.tool_input.file_path; if ($f -and (Test-Path $f)) { cd 'D:\\projet rima\\pronunciation-main\\pronunciation\\model-train'; $rel = $f -replace [regex]::Escape('D:\\projet rima\\pronunciation-main\\pronunciation\\model-train\\'), ''; git add $rel 2>$null; $name = Split-Path $rel -Leaf; git commit -m \"auto: update $name\" 2>$null; git push origin main 2>$null }",
            "timeout": 30,
            "statusMessage": "Push vers GitHub...",
            "async": true
          }
        ]
      }
    ]
  }
}
